spring.application.name=backend
spring.datasource.url=${DB_URL:jdbc:mariadb://localhost:3306/tesvg_red_social}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:1234}
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=30MB
spring.servlet.multipart.max-request-size=60MB

app.upload.dir=${UPLOAD_DIR:/home/robe/uploads}
app.jwt.secret=${JWT_SECRET:mi_clave_super_secreta_12345678901234567890}
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:8080,http://localhost:3000,http://localhost:3001,http://127.0.0.1:8080,http://127.0.0.1:3000,http://127.0.0.1:3001,http://192.168.*.*:3000,http://192.168.*.*:3001,http://192.168.*.*:8080,http://10.*.*.*:3000,http://10.*.*.*:3001,http://10.*.*.*:8080,http://172.16.*.*:3000,http://172.16.*.*:3001,http://172.16.*.*:8080}

spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:}
spring.data.redis.timeout=${REDIS_TIMEOUT:1500ms}
spring.cache.type=redis
spring.cache.redis.time-to-live=${REDIS_CACHE_TTL:60s}

app.redis.enabled=${REDIS_ENABLED:true}

spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.serialization.write-date-timestamps-as-nanoseconds=false
server.address=0.0.0.0

# ── Pure API mode: disable legacy static HTML serving ──────────────────────
# Next.js (port 3000) is the sole frontend. Spring Boot (port 8080) = API only.
# ImagenController serves /imagenes/** via its own @GetMapping (unaffected).
spring.web.resources.add-mappings=false

app.vapid.public-key=${VAPID_PUBLIC_KEY:BGjII3SLb-vktf05B2c81C7P0ZpNGRqg16o77V39dwKcHCVX4skG8GJcAQDXhRSjgLT_AfIhxnr1MhkylwX01os}
app.vapid.private-key=${VAPID_PRIVATE_KEY:8qx3JBIZsoC0q4PXd2earQIbBWbsIMFOaah04PyPQIE}
app.vapid.subject=mailto:nido@tesvg.edu.mx

# ── Debug logging para diagnóstico de seguridad ───────────────────────────────
logging.level.com.tesvg.backend.security=DEBUG
logging.level.com.tesvg.backend.config=DEBUG
logging.level.org.springframework.security=WARN
